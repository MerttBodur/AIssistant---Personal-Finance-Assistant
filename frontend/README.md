# AIssistant Frontend

Next.js App Router frontend for the AIssistant hackathon demo.

This folder owns the mobile fintech UI only. Mock JSON belongs in the root `mock-data/` folder, API routes belong in `api/`, and AI/Gemini work belongs in `ai/` when those feature branches are merged.

Run from this folder:

```bash
npm install
npm run dev
```
